export enum customer_class_enum {
  STUDENT = "طالب",
  CLIENT = "عميل عادي",
  VIP_CLIENT = "عميل مهم",
}

export enum order_method_enum {
  DELIVERY = "دليفري",
  ONSITE = "محل",
}

export enum order_type_enum {
  SELL = "بيع",
  RETURN = "استرجاع",
}
